<!--Grievance cell members-->
<!--Admin-->
<div ng-app="grievances" ng-controller="Admin_Rejected_user" ng-init="init('staff')">
    <h1>Rejected Non Teaching Staff List</h1>
    
    <div id="rejected_list" class="contenttable">
        <table class="table table-hover table-striped">
            <thead class="table-success">
                <tr>
                    <th>S.No.</th>
                    <th>Name</th>
                    <th>Gender</th>
                    <th>Department</th>
                    <th>Designation</th>
                    <th>Employee ID</th>
                    <th>Email ID</th>
                    <th>Mobile No.</th>
                    <th>Action<th>
                </tr> 
            </thead>
            <tr ng-repeat="user in users">
                <td>{{$index+1}}</td>
                <td>{{user.name}}</td>
                <td>{{user.gender}}</td>
                <td>{{user.dep}}</td>
                <td>{{user.Desig}}</td>
                <td>{{user.id}}</td>
                <td>{{user.emailid}}</td>
                <td>{{user.mobileno}}</td>
                <td>
                <button ng-click="Undo_rejected('Staff',user.emailid)" class="btn btn-success">
                    Undo Reject
                </button>
                </td>
            </tr>
        </table>
    </div>
    <div id="loading">
        <div class="loader">
        </div>
        
        <h1>Loading...</h1>
    </div>

</div>


<!--Grievance cell members-->
<!--Admin-->
<div ng-app="grievances" ng-controller="Admin_Rejected_user" ng-init="init('parent')">
    <h1>Rejected Parents List</h1>
    <div id="rejected_list" class="contenttable">
        <table class="table table-hover table-striped">
            <thead class="table-success">
                <tr>
                    <th>S.No.</th>
                    <th>Name</th>
                    <th>Email ID</th>
                    <th>Mobile No.</th>
                    <th>Relationship</th>
                    <th>Action</th>
                </tr> 
            </thead>
            <tr ng-repeat="user in users">
                <td>{{$index+1}}</td>
                <td>{{user.name}}</td>
                <td>{{user.emailid}}</td>
                <td>{{user.mobileno}}</td>
                <td>{{user.relation}}</td>
                <td>
                <button ng-click="Undo_rejected('Parent',user.emailid)" class="btn btn-success">
                    Undo Reject
                </button>
                </td>
            </tr>
        </table>
    </div>
    <div id="loading">
        <div class="loader">
        </div>
        
        <h1>Loading...</h1>
    </div>

</div>


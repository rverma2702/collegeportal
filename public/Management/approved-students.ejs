Approved<!--Grievance cell members-->
<!--Management-->
<div ng-app="grievances" ng-controller="Management_apprv_user" ng-init="init('student')">
    <h1>Approved Students List</h1>
    <div id="apprv_list" class="contenttable">
        <table class="table table-hover table-striped">
            <thead class="table-success">
                <tr>
                    <th>S.No.</th>
                    <th>Name</th>
                    <th>Course</th>
                    <th>Batch</th>
                    <th>Hall Ticket</th>
                    <th>Email ID</th>
                    <th>Mobile No.</th>
                </tr> 
            </thead>
            <tr ng-repeat="user in users">
                <td>{{$index+1}}</td>
                <td>{{user.name}}</td>
                <td>{{user.dep}}</td>
                <td>{{user.Batch}}</td>
                <td>{{user.id}}</td>
                <td>{{user.emailid}}</td>
                <td>{{user.mobileno}}</td>
            </tr>
        </table>
    </div>
    <div id="loading">
        <div class="loader">
        </div>
        
        <h1>Loading...</h1>
    </div>

</div>

